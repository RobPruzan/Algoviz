import React from 'react';

const Loading = () => {
  // this does run need a sensible loading state
  return <div className="bg-white text-6xl w-full h-full">I AM LOADING</div>;
};

export default Loading;
